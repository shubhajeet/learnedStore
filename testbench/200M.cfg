# config.cfg
# EXP_CONFIG_FILE=$(basename "$0")
NUM=200000000
# DRAM=1
# DRAM=50
DRAM=100
# DRAM=0.1
STEP=0
# READ=50000000
# DRAM=200
# DRAM=225
# READ=1000000
READ=${NUM}
READTIME=300
COLLECT_STATS=false
# MODE=Debug
MODE=Release
# MODE=RelWithDebInfo
WORKERS=1 
PP_THREADS=2
MAX_ERROR=100
SEQ_OPS=false
# SSD_FILE=250M_ssd
# META_FILE=leanstore_250M.json
# TRACE_FILE=randomtrace_250M.data
# STATS_LOC=../stats/in_mem_readseg
# ATTACH_SEG_FILE=250M_attach_seg.bin
# SPLINE_FILE=250M_spline.bin
# MAPPING_FILE=250M_mapping.bin
SSD_FILE=${conf%.cfg}_ssd
META_FILE=leanstore_${conf%.cfg}.json
TRACE_FILE=randomtrace_${conf%.cfg}.data
STATS_LOC=../stats/${conf%.cfg}_${exp}
ATTACH_SEG_FILE=${conf%.cfg}_attach_seg.bin
SPLINE_FILE=${conf%.cfg}_spline.bin
MAPPING_FILE=${conf%.cfg}_mapping.bin
PROF=../profile_data/${conf%.cfg}_${exp}
